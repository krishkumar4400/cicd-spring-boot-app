name: spring-boot-app-demo

on:
    push:

jobs:
    build-test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              run: echo "checkout code"

            - name: Setup JDK
              run: echo "setup jdk"

            - name: Build And Test (Maven)
              run: echo "build and test"

            - name: Upload Test Reports
              run: echo "upload test reports"

    docker-build-push:
        runs-on: ubuntu-latest
        needs: build-test

        steps:
            - name: Checkout code
              run: echo "checkout code"

            - name: Setup JDK
              run: echo "setup jdk"

            - name: Build JAR
              run: echo "build jar"

            - name: Login to Docker Hub
              run: echo "docker login"

            - name: Push to Docker Hub
              run: echo "push image to docker hub"
